<task-notification>
<task-id>ada3607481f341f81</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/8d0af0b1-c97c-4315-abcd-33fb892c7f6d/tasks/ada3607481f341f81.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 18 (AuthenticateApiKey)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>**STATUS:** DONE — cache-hit auth-bypass closed.

**Fix commit:** `55117d8` — `fix(rs): verify api-key secret on cache hit to close auth bypass (SMA-445)` (SSH-signed, verified). Sits on top of the original `d99c350`.

**What changed (Option A):** `CachedValidation` now carries `key_hash` (the same peppered HMAC already in Postgres — base64 in the Redis wire form; safe to cache, useless without the in-process pepper). The cache-HIT path now constant-time-verifies the presented secret against `cached.key_hash` FIRST and denies `InvalidToken(BadSecret)` on mismatch (no eviction — a wrong-secret guess is an attack, not a revocation). The hit still saves the two DB round-trips; it just never skips the secret check. The miss path caches the hash it already fetched.

**Test output (Docker up):**

Integration (`--test api_key_auth`), 7/7 passed:
```
PASS cache_hit_with_wrong_secret_is_denied      &lt;-- THE regression test
PASS cache_hit_still_authenticates_a_since_revoked_key_within_ttl
PASS revoked_key_denied
PASS expired_key_denied
PASS disabled_sa_denies_live_key
PASS wrong_secret_denied
PASS valid_key_resolves_to_sa_principal
```

Unit (`authenticate_api_key`), 14/14 passed — including `cache_hit_with_wrong_secret_is_denied`, `cache_hit_skips_db_but_still_verifies_secret` (renamed; proves the DB is NOT hit on a hit via `PanicIfCalled*` repos, yet the secret IS verified), plus the disabled/revoked/expired denials all green.

Full crate: `cargo nextest run -p paigasus-iam` → 387/387 passed. `cargo build` / `clippy --all-targets -D warnings` / `fmt --check` all clean. Report appended at `.superpowers/sdd/task-18-report.md`.</result>
<usage><subagent_tokens>261414</subagent_tokens><tool_uses>40</tool_uses><duration_ms>766961</duration_ms></usage>
</task-notification>