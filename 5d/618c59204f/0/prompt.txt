<task-notification>
<task-id>a2de1d707f0f7df2b</task-id>
<tool-use-id>toolu_01BUdP5hjwX1t3Nkuwo4Rvyy</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/5cf80dca-6929-4a4c-aca9-d08cfa10fde9/tasks/a2de1d707f0f7df2b.output</output-file>
<status>completed</status>
<summary>Agent "Second challenge of the SMA-560 spec" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I've verified the spec's load-bearing claims against the code. Here is the review.

## Verdict: APPROVE WITH CHANGES

Rev 2 is a substantial and honest improvement: the rev-1 falsehood is retracted with the correct citation, §1.2's three holes all verify against `run.sh` (I confirmed deleting the wasm glob from `paigasus-kernel-ts:test` is green today), §4.3's under-declaration is real and the fix is exactly sufficient, and the second floor is the right response to the vacuity mode. What blocks it is that the two new floors do not close a third vacuity mode, and §5 item 10's "no wiring is needed" is false in a way that leaves the new `repo:affected-smoke` input as the one unpinned entry in the repo's most load-bearing input list.

## Findings

### BLOCKER

- **§5 item 10 is wrong: D3's new input requires wiring in two other files, and skipping it silently un-pins it** — `repo:affected-smoke`'s inputs are pinned by check 8e's `T_AFFECTED_SMOKE_REQUIRED_INPUTS` (`/Users/smaschek/dev/paigasus/paigasus-core/ci/actionlint/run.sh:2097-2117`, 19 entries — exactly the 19 in `moon.yml:165-202`) plus an arity floor `-ge 19` (`ci/actionlint/run.sh:4433`), whose literal text is in turn pinned verbatim by `ci/affected-graph/ci_targets.py:572`. Check 8e is **containment**, so adding a 20th input stays green — but `run.sh:2087-2096` states the table's contract explicitly: *"The set below is the WHOLE current list rather than a judged subset — a floor, not a judgement call."* | Why it matters: `rs/crates/*/*/build.rs` would become the only `repo:affected-smoke` input whose deletion nothing reds, and D3's entire argument is that an unreachable assertion is worthless. It is also self-concealing in the same way removing `- 'moon.yml'` is. | Suggested fix: state in §5 that the change is a three-file edit — `moon.yml` inputs, `T_AFFECTED_SMOKE_REQUIRED_INPUTS` (→ 20 entries), the arity floor `19`→`20` at `ci/actionlint/run.sh:4433`, **and** the matching verbatim string in `ci_targets.py`'s `ACTIONLINT_SH_CALL_SITES` (`:572`). Keep the "27 targets, no new `repo:*` task, no `T`/CLAUDE.md/`SELF_SCHEDULED_GATES` change" claim — I verified `.github/workflows/ci.yml:214` has 27 entries and CLAUDE.md's marker block matches — but drop the "no wiring is needed" generalisation.

- **A third vacuity mode survives both floors: the floor constants are themselves unfloored and mutually unchecked** — For A7, `REQUIRED_WRAPPER_TASKS` *is* the examined set (D6), so emptying it makes A7 iterate nothing and print PASS. `cargo_moon_parity.py:555-560` already establishes the required countermeasure for exactly this (`if not REQUIRED_FFI_TASKS: failures.append("REQUIRED_FFI_TASKS is empty — A5's floor would assert nothing")`), and the comment there records that A6's self-test passes explicit `floor=` args so the real constant is *never* otherwise exercised. Separately, nothing ties the two new constants together: deleting `paigasus-kernel-py` from `REQUIRED_WRAPPER_CLOSURE` while leaving `paigasus-kernel-py:test` in `REQUIRED_WRAPPER_TASKS` leaves that wrapper examined with an unfloored derivation — the py half goes vacuous while the ts half keeps the run green. Third gap: the spec never says what happens when a floor entry names an absent project or task; A6 handles this with `_ABSENT` (`:362-381`) and a `not in examined` row (`:339-343`), A5 with `set(floor) - matched` (`:271-275`). | Why it matters: this reproduces the exact failure class the spec cites (SMA-436/489/528/541/553) one level up, in the constant that is supposed to prevent it. | Suggested fix: (a) add explicit non-emptiness self-test rows for both constants, mirroring `:555-560`; (b) collapse them into ONE table keyed by wrapper (`{"paigasus-kernel-ts": {"tasks": (...), "closure": {...}}}`) or add a self-test row asserting `{t.split(":")[0] for t in REQUIRED_WRAPPER_TASKS} == set(REQUIRED_WRAPPER_CLOSURE)`; (c) specify that a floor entry naming a missing project/task, or a task reporting `None` inputs, is a `FLOOR:`-prefixed violation, never a skip.

### MAJOR

- **The SMA-537 and SMA-560 halves intersect at `paigasus-node-bindings/build.rs`, and the spec closes neither side of it** — `paigasus-kernel-ts:{build,test}` run `pnpm exec napi build --cwd ../../../rs/crates/bindings/paigasus-node-bindings` (`ts/packages/paigasus-kernel/moon.yml:43,120`), which compiles that crate's `build.rs` (`napi_build::setup()` — it emits the addon's link args). Neither wrapper task keys on it, and A7 as specified demands only `&lt;upstream&gt;/src/**/*` + `&lt;upstream&gt;/Cargo.toml`, so it never will. D2 fixes the *Rust* crate tasks only. | Why it matters: this is a live under-declaration of exactly the §1.2-item-3 shape, in the same PR that fixes the other one, and it is the one file where both issues in scope actually meet. | Suggested fix: extend §4.3 to add `/rs/crates/bindings/paigasus-node-bindings/build.rs` to `paigasus-kernel-ts:{build,test}` alongside the kernel `Cargo.toml`, and extend A7's `want` to include `&lt;upstream&gt;/build.rs` for closure members that have one on disk (the same disk scan D2 already needs).

- **D4 asserts only two of the four new `fmt` inputs; the two that matter most stay unasserted** — `check_lint_inputs` reads `task_inputs` only (`cargo_moon_parity.py:216-230`), i.e. moon's `inputFiles`. `rustfmt.toml` and `rust-toolchain.toml` are literals and land there, but `@group(sources)` and `@group(tests)` resolve to **globs** and land in `inputGlobs` (`moon_projects()`, `:441-448`; A6 exists precisely because a one-bucket read could never see half of every pair, `:744-762`). So a `check_task_inputs` modelled on A4 cannot see `@group(tests)` at all. | Why it matters: §D4's own thesis is "a spec whose thesis is *unasserted inputs rot* must not add four unasserted inputs" — and as written it adds two. Removing `@group(tests)` from `fmt` later restores today's bug with nothing red. | Suggested fix: have `check_task_inputs` union both buckets like A6 (`:381`), and require `&lt;crate&gt;/src/**/*` and `&lt;crate&gt;/tests/**/*` on `fmt` as well as the two literals.

- **D2's new `build.rs` assertion has no floor and no call-site pin** — it is a *derived* check (crates with a `build.rs` on disk), and the spec applies the anti-vacuity discipline only to A7 (§4.4). Cargo lets a package relocate its build script via `[package] build = "custom.rs"`; the disk scan then finds nothing, the derived set empties, and the assertion prints PASS while the script still exists and still matters. This is the same shape as A5's `REQUIRED_FFI_TASKS` (`:96-107`). | Why it matters: the spec rejects "per-crate with no assertion" on SMA-524 grounds and then ships an assertion that can silently become no assertion. | Suggested fix: add `REQUIRED_BUILD_RS_CRATES = {"paigasus-node-bindings-rs"}` with a membership row, its own non-emptiness self-test row, and honour `[package] build` when scanning.

- **§4.4 names the requirement but no mechanism, and misses the sharper bypass** — `cargo_moon_parity.py` has no source-pinning precedent (the only in-repo Python precedent is `inspect.signature(...)` default-pinning at `ci_targets.py:1732,1881`). More importantly, the real SMA-542-shaped hole here is `main()`'s aggregate guard: `if not (a1 or a2 or a3 or a4 or a5 or a6)` (`:897`) and the report tuple (`:907-941`). Calling `check_wrapper_upstream_inputs()` and forgetting to fold `a7` into *both* is a green no-op that a call-site-only pin does not catch. | Why it matters: the cited lesson is that the check's own call site goes unguarded; here the guard line is a second, equally unguarded site. | Suggested fix: specify the mechanism (a self-test row over `inspect.getsource(main)` requiring both `check_wrapper_upstream_inputs(` and `a7` inside the `if not (...)` expression), or restructure `main()` to iterate a list of (rows, title) pairs so a forgotten fold is impossible.

- **A materially simpler D2 exists and is not weighed against the right precedent** — Adding `build.rs` to `.moon/tasks/rust.yml`'s `sources` fileGroup (`:8-10`) is one line; it reaches `build`, `test`, `lint`, `build-release` **and** `fmt` (all consume `@group(sources)`), covers every future crate automatically, and needs no new assertion, no new `repo:affected-smoke` input, and none of the check-8e churn the BLOCKER above describes. `.moon/tasks/rust.yml:50-56` makes exactly this argument for `deps: ['^:build']` on `lint`: *"Declared here rather than per-crate so a new crate has no per-crate `lint` declaration to forget."* The spec's rejection rests on a future widening of `repo:input-liveness` past `repo:*` — which has not landed (I confirmed `task_inputs.py` is `repo`-scoped) and may never. | Why it matters: the chosen route costs a new assertion, a new floor, a new gate input and a three-file pin update, to buy the same coverage the one-liner gets for free. | Suggested fix: either adopt the shared template, or strengthen the rejection with a concrete cost the one-liner actually incurs today (I could not find one).

- **D6/L2 re-creates the SMA-524 failure the spec cites against it, and the fix is already in the file** — `REQUIRED_WRAPPER_TASKS` is byte-identical to `REQUIRED_FFI_TASKS` (`cargo_moon_parity.py:103-107`). A5 already **derives** that set from the task's resolved invocation via `FFI_MARKERS` (`:94`, `check_ffi_inputs` `:233-276`) — `napi build`, `wasm-pack`, `maturin`, `--reinstall-package` — and already floors it, already self-tests the floor's non-emptiness, and already has a "not matched by any FFI marker" row. A new wrapper's `napi build` task is matched by that derivation on day one **even if it declares zero inputs**, which is exactly the bug L2 admits nothing detects. | Why it matters: L2 and L4 both dissolve, `REQUIRED_WRAPPER_TASKS` disappears, and one of the two BLOCKER sub-problems (an unfloored floor) goes with it. | Suggested fix: split a `derive_ffi_tasks(projects) -&gt; set[str]` helper out of `check_ffi_inputs`, have A7 examine `{t for t in derive_ffi_tasks(...) if projects[t.split(":")[0]]["language"] != "rust"}`, and keep `REQUIRED_FFI_TASKS` as the shared floor.

- **A7's read shape and scope are unspecified in the one place it matters** — §D5 never says A7 reads *both* `inputFiles` and `inputGlobs`, nor that `observed` is computed **per task** rather than unioned across the wrapper's tasks. An `inputGlobs`-only A7 makes §4.3's kernel `Cargo.toml` fix permanently invisible; an `inputFiles`-only A7 makes §5 item 3's headline mutation (delete the wasm glob) a **vacuous pass**; a union-across-tasks A7 also passes that mutation, because `:build` keeps the glob. | Why it matters: two of the three wrong readings fail the spec's own acceptance test silently rather than loudly. | Suggested fix: state explicitly, citing `cargo_moon_parity.py:362-381`, that A7 unions both buckets per `(project, task)` and treats a `None`/absent bucket as a violation.

- **The M7 fallback silently drops `fmt`** — `fileGroups.upstreams` is consumed by `build`/`test`/`lint` only; `.moon/tasks/rust.yml:22-23` says so explicitly ("Deliberately NOT on `fmt`"). D2 requires `build.rs` on all four tasks, so the "actually-cheap fallback" would red D2's own assertion for `paigasus-node-bindings-rs:fmt`. It is also semantically wrong: `upstreams` is documented everywhere as "the transitive dependsOn closure", and this puts the crate's own file in it. | Why it matters: the fallback is the plan's answer if R1 comes back "replace", i.e. it gets used under time pressure. | Suggested fix: note the `fmt` gap in R1's fallback, or drop the fallback in favour of the shared template.

### MINOR

- **Open question 2 is already answered by the tree; it should not cost a plan cycle** — `@group(tests)` is already on the inherited `test` (`.moon/tasks/rust.yml:39`) and `lint` (`:77`) for all 13 crates, several of which have no `tests/` dir, and those tasks pass today. `repo:input-liveness` does not examine crate tasks. An empty group is a glob matching nothing, not a violation. Close it in the spec.
- **§5's "the re-baseline" paragraph is answerable now, not by measurement** — no `run.sh` case touches `rs/rustfmt.toml`, `rs/rust-toolchain.toml`, `rs/crates/libs/paigasus-kernel/Cargo.toml`, or any `build.rs`, and `fmt` is filtered out of `_assert_task_case_impl` (`run.sh:96`). No expected set moves. Say so; the ad-hoc probe is still worth running, but the plan should not carry this as an unknown.
- **§1.1's parenthetical over-states `repo:input-liveness`** — it says the shared-template option produces "the untracked-input class `repo:input-liveness` reds on". It does not red on it today (the gate is `repo:*`-scoped). The sentence half-qualifies this in the next clause; make the tense unambiguous.
- **R1 should scope its measurement** — no crate declares `inputs:` on an inherited Rust task (I checked all 13 `rs/crates/*/*/moon.yml`), so there is genuinely no in-repo evidence for `inputs` merge semantics. But `deps` append-merge *is* documented in-repo at `rs/crates/libs/paigasus-proto/moon.yml:35-37` ("merges in via Moon's append-merge"). R1 should cite that and say the measurement is needed because Moon merges per-field, not because merge behaviour is wholly unknown.
- **Open question 3 has a determinable answer** — since no `run.sh` expected set moves (above), there is no reason to split §4.3 from A7; one commit avoids ever making "A7 reds on a pre-existing defect" real.

### QUESTIONS

- Does A7 apply an `own/`-prefix exclusion to `observed` as A6 does (`:400-401`)? For a ts/py wrapper `source_dir` is `ts/packages/...`, so the `rs/crates/` filter alone suffices — but if the code is copied from A6 verbatim the `own` variable becomes dead, which is worth stating rather than leaving to the reader.
- §4.3 says "plus each binding crate's `Cargo.toml` where absent". I verified all three binding manifests are already declared, so this clause is a no-op today. Is it intended as forward-looking guidance, or did it survive from an earlier draft where one was missing?
- §2.2 defers SMA-560's codegen case ("a `buf.gen.yaml` or plugin-version change regenerates output without touching a `.proto`") to a new issue. Is that issue filed? The spec's References list every other cited issue by key; this one is the only "needs a new issue" with no key.
- D5 justifies containment over strict-equality using `package.json`/`pyproject.toml` under `rs/crates/`, but the larger over-declaration is `/rs/crates/libs/paigasus-kernel-parity/vectors/**/*` on all three tasks — a whole crate dir not in the closure. Does the containment argument still read the same way once that is the headline example, or does it strengthen the case for strict-equality-plus-one-waiver?

## What's good

- **The rev-1 retraction is exemplary.** §1.2's "Correction to rev 1 (B3)" names the false claim, cites the line that disproves it (`run.sh:343`), and then rebuilds the justification from scratch rather than quietly deleting the sentence. I independently verified all seven rows of the §79 "Verified against `main`" table and the three uncovered holes — including that deleting the wasm glob from `paigasus-kernel-ts:test` really is green today (project-level `binding-oneway-wasm` survives via `:build`'s own glob; A5 still matches the markers; A6 skips the project on `language != "rust"`). Preserve that table and the correction section verbatim.
- **§4.3 is correct and exactly sufficient.** Only the kernel's `Cargo.toml` is missing, from exactly three tasks, and adding it moves no `run.sh` expected set. The "land it before or with A7, or the temptation is to weaken A7" reasoning is the right instinct and worth keeping as a stated rule.
- **D5's two-floor argument is the right diagnosis.** Recognising that a containment check with a derived `want` is vacuously satisfied when `want` empties — and that a task-name floor is silent on that — is precisely the reasoning `cargo_moon_parity.py:129-132` encodes for A6. The `FLOOR:`-prefix requirement and the warning that "emptying `deps` also empties `want`, producing confusable rows" show the author read the existing self-test's hard-won comment at `:801-810` rather than reinventing it.</result>
<usage><subagent_tokens>182747</subagent_tokens><tool_uses>27</tool_uses><duration_ms>568565</duration_ms></usage>
</task-notification>