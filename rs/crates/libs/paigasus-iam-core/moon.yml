$schema: 'https://moonrepo.dev/schemas/project.json'

id: 'paigasus-iam-core-rs'
layer: 'library'
language: 'rust'

dependsOn:
  - 'paigasus-kernel-rs'

tasks:
  build:
    deps: ['^:build']
  test:
    deps: ['^:build']
