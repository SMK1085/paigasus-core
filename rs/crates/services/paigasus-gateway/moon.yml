$schema: 'https://moonrepo.dev/schemas/project.json'

id: 'paigasus-gateway-rs'
layer: 'application'
language: 'rust'

dependsOn:
  - 'paigasus-proto-rs'
  - 'paigasus-kernel-rs'

tasks:
  build:
    deps: ['^:build']
  test:
    deps: ['^:build']
