dist
.next
node_modules
pnpm-lock.yaml
coverage
next-env.d.ts
generated
# Playwright's own run artifacts (packages/paigasus-auth's E2E tier, task 13) — never committed,
# see the root .gitignore entry.
test-results
playwright-report

# Project memory: hand-wrapped prose, like the root CLAUDE.md. Prettier strips the
# continuation indent and breaks the list nesting.
CLAUDE.md
