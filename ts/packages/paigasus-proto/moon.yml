$schema: 'https://moonrepo.dev/schemas/project.json'

id: 'paigasus-proto-ts'
layer: 'library'
language: 'typescript'

tasks:
  build:
    deps: ['contracts:generate']
  typecheck:
    deps: ['contracts:generate']
  test:
    deps: ['contracts:generate']
