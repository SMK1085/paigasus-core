<task-notification>
<task-id>ae213f7f024c5000c</task-id>
<tool-use-id>toolu_01QHCbuY1gs89tK14W7R4NCX</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/a19cb7d1-7551-4054-ba7a-9e80a3de0013/tasks/ae213f7f024c5000c.output</output-file>
<status>completed</status>
<summary>Agent "Adversarial spec challenge" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I've read the spec and ground-truthed it against `run.sh` (all 1873 lines of the relevant regions), `ci_targets.py`, `ci.yml`, `moon.yml`, and the README. Here is the review.

## Verdict: NEEDS REWORK

The problem statement is real and the cycle topology (D1) is the right shape, but three load-bearing claims do not survive contact with the code: `repo:affected-smoke`'s `inputs` do not cover `ci/actionlint/run.sh`, so the new C4 pin never runs on the PR that trips it (and D9's "no moon.yml change" is therefore false); the D6 control has no stated deletion precondition, which under D5's own "no recursion guard" decision is an unbounded self-spawning loop; and the battery's kill criterion ("non-zero exit") cannot distinguish the counter firing from an infra exit. The `--self-test` cost story is also hand-waved for a path the README advertises as "for fast iteration," and there is a materially simpler restructure that removes D5 entirely.

## Findings

### BLOCKER

- **C4's new pin is unreachable on the exact PR it exists to catch** — §4 adds `ACTIONLINT_SH_CALL_SITES` to `ci_targets.py`, which runs inside `repo:affected-smoke`. That task's `inputs` (`/Users/smaschek/dev/paigasus/paigasus-core/moon.yml:130-155`) list `ci/affected-graph/**/*`, `.github/workflows/ci.yml`, `.moon/**/*`, the `moon.yml`s, manifests, `CLAUDE.md`, `.prototools` — **not** `ci/actionlint/**`. A PR whose only change is deleting `run_self_tests` and `selftest_mutation_battery` from `ci/actionlint/run.sh` leaves `repo:affected-smoke` unaffected, so `moon ci --base origin/main` never schedules it, C4 never executes, and `repo:actionlint` (which *is* scheduled, `inputs: ['**/*']`) now runs zero checks and exits 0. The deletion ships green — the precise defect the spec was filed for, reproduced by the fix. | Why it matters: the primary AC-1 mechanism is a no-op on its own threat model. | Suggested fix: add `'ci/actionlint/**/*'` to `repo:affected-smoke`'s `inputs` in `moon.yml`, state it in §4, and delete D9's claim that "the diff [is] two scripts plus documentation." I verified no cascade case in `ci/affected-graph/run.sh` (lines 193-270) keys on `ci/`, so this re-baselines nothing — it is a one-line change, but it must be in the spec.

- **The D6 control has no deletion precondition, and D5 removed the only other recursion guard** — D5 states the precondition for *mutants* ("The battery asserts exactly two lines were removed before spawning anything") and explicitly rejects a `PAIGASUS_ACTIONLINT_MUTANT=1` guard on the grounds that "the recursion is structurally impossible once the line is gone." D6 then introduces a sixth artifact — `run.sh` with *only* the battery line removed — and states no precondition for it. If that `sed` ever matches zero lines (a reformatted invocation, an added `|| FAILED=1` suffix, a re-indent), the "control" is byte-identical to `run.sh`, and `bash &lt;control&gt; --self-test` re-enters `selftest_mutation_battery`, which builds another identical control, forever. Worse, the spec does not say whether the battery validates all six preconditions *before* spawning or interleaves validate-then-spawn per artifact — under the interleaved reading the five mutants fail their preconditions loudly and the control still recurses. | Why it matters: an unbounded process-spawning loop inside a gate with `inputs: ['**/*']` that runs on every CI job; it hangs the required check rather than redding it. | Suggested fix: state that the control asserts **exactly one** line removed, that **all six** preconditions are validated before any subprocess is created, and that a precondition failure aborts the battery. Add a belt-and-braces depth guard that is not a bypass (e.g. an unexported counter var checked at the battery, or `[ -t 0 ]`-free `PAIGASUS_ACTIONLINT_DEPTH` that only ever *increments* — it cannot switch the gate off, only refuse to nest).

### MAJOR

- **"Require a non-zero exit" cannot distinguish the counter firing from an infra abort** — §4 and D5 specify only "require a non-zero exit." `run.sh` has three non-zero exits: `fail()`→`exit "$FAILED"`=1, `infra()`→`exit 2` (`run.sh:34-37`), and `no_origin_main_infra` → `exit 2` from inside `branch_filter_self_test` itself (`run.sh:1392`). A mutant that dies at exit 2 for a transient reason (a `git for-each-ref` hiccup, `mktemp` failure, exit 127 from a missing mutant file) is scored "correctly killed." D6's control catches the *systematic* version of this, but not the per-mutant/nondeterministic version, and the spec's D6 rationale only claims to catch "a battery whose `bash` invocation was broken." | Why it matters: the battery is AC-1's sole standing proof; a false "killed" is exactly the silent pass this issue is about. | Suggested fix: require `rc == 1` **and** that the mutant's stderr contains `assert_self_tests_ran`'s distinctive message. Treat `rc == 2` as its own reported outcome ("mutant aborted before the assertion"), not a kill.

- **`$0` is not a safe mutant source after `run.sh:25`** — `run.sh` does `cd "$(git rev-parse --show-toplevel)"` on entry. §4 says only "Mutants are written under `mktemp -d`" and never names the source file. If the implementer uses `$0`, then `ci/actionlint/run.sh` (Moon's invocation, `moon.yml:508`) works, but `cd ci/actionlint &amp;&amp; ./run.sh` gives `$0='./run.sh'`, which no longer resolves after the `cd`; `cp` fails silently under `set -uo pipefail` (no `set -e`), `bash &lt;missing&gt;` exits 127, and all five mutants score as "killed" — a false pass on AC-1, with only the control redding, and with a nonsense message. | Why it matters: silent false pass in the wrong-cwd case; misleading red in the caught case. | Suggested fix: capture the absolute self path **before** the `cd`, e.g. `SELF_SRC="$(cd "$(dirname "${BASH_SOURCE[0]}")" &amp;&amp; pwd)/$(basename "${BASH_SOURCE[0]}")"` at ~`run.sh:24`, and say so in §4. Also state that `cp` and `sed` failures are checked, not assumed.

- **D2 makes `branch_filter_self_test`'s global-state restore load-bearing for production checks 5/6, with no control** — `branch_filter_self_test` mutates three globals and restores them: `BRANCH_SKIP` (`run.sh:1425-1428`) and `ORIGIN_REFS`/`ORIGIN_REFS_LOADED` (`run.sh:1438-1444`, deliberately poisoned with a `main`-free list to reach the `no-origin-main` token). Today those poisons are harmless because the self-tests run *last* (`run.sh:1868-1871`), after the production call site's `load_origin_refs` (`run.sh:1771`) and after every `branch_verdict`. Under D2 they run *first*, and because `load_origin_refs` early-returns on `ORIGIN_REFS_LOADED=1` (`run.sh:1093`), a botched restore would silently feed checks 5/6 a fake ref list — turning every real `branches:` entry into `no-origin-main` (infra exit 2) or a false `unresolved`. The spec never mentions this coupling. | Why it matters: a plausible future edit to that fixture silently corrupts the gate's only production assertion, in the direction of a false red *or* a false skip depending on the fake list. | Suggested fix: have `run_self_tests` reset `ORIGIN_REFS_LOADED=0` (and re-assert `BRANCH_SKIP` is empty) after the block, before checks 5/6, and record why. Alternatively state the invariant explicitly and add it to §6.

- **D2 silently inverts the documented lazy-canary contract** — `run.sh:1186-1191` states as a design property: *"The canary is LAZY (D7) — only an entry that has survived every filter above actually needs a ref, so checks 1-6 still run to completion and report their own findings before this canary ever fires."* Running the self-tests first makes `branch_filter_self_test`'s unconditional precondition (`run.sh:1391-1392`) exit 2 before `actionlint` is ever invoked. On a `--depth 1` / `--single-branch` checkout a developer now loses every check-1..6 finding. The spec's D2 lists three properties and this is not among them. | Why it matters: a documented invariant becomes false and the comment becomes a lie; DX regression on shallow clones. | Suggested fix: either state the change and update `run.sh:1186-1191` and the README's "Running it" note, or place `run_self_tests` after checks 1-6 and put only `assert_self_tests_ran` + the battery at the end (which still satisfies "a rotted checker must red").

- **The 5-mutant battery is ~6× the gate's standalone cost for ~1× the coverage, and there is a materially simpler design** — `--self-test` today runs 84 fixture invocations (`run.sh`: 43 `check_fixture`, each forking `mktemp` + `awk`; 11 `expect_pattern`, most forking `git ls-files` via `tracked_exact` at `run.sh:964-970`; ~11 `expect_branch`, several forking `git check-ref-format`; 5 `expect_config` forking `mktemp`+`grep`+`awk`+`sort`), plus `git for-each-ref`. Call it 150-250 forks. Check 9 multiplies that by six, on **every** full gate run *and* on `--self-test`, which `README.md:125` advertises as "the four fixture tables only, for fast iteration." §5 defers measurement to implementation ("if it materially changes the through-Moon figure, the README says so"). It will materially change the ~1.0s standalone figure — 4-6× — and it makes the iteration loop the slowest path. Separately: all five mutants exercise **one** mechanism (`SELF_TESTS_RAN != SELF_TEST_COUNT`); deleting any of the five lines produces the identical count-4 failure, so mutants 2-5 add no discriminating power beyond the (free, subprocess-less) precondition that each line exists exactly once. | Why it matters: cost with no coverage return, on the path documented as fast. | Suggested fix, and it also kills D5 and the BLOCKER above: move `selftest_mutation_battery` **after** the `SELF_TEST_ONLY` early exit. Then `--self-test` = tables only (fast, unchanged), the full gate runs the battery, mutants are invoked with `--self-test` and **cannot recurse by construction** — no two-line deletion, no control-recursion hazard, one-line `sed`. Full-gate-only also matches the convention D2/§4 already cite for check 8. Then run the five *preconditions* (cheap, textual) but spawn only one mutant + the control: 2 subprocesses instead of 6.

- **Mutant output is unspecified and will pollute every CI run** — each killed mutant prints its own `fail()` lines, which are prefixed `actionlint gate:` (`run.sh:30`) and therefore indistinguishable from the real gate's output. With `.moon/tasks.yml`'s `buffer-only-failure`, five mutants' worth of "actionlint gate: …" noise surfaces on *any* unrelated failure of this task, which is precisely when the log needs to be readable. | Why it matters: observability regression in the required check. | Suggested fix: §4 must state that mutant stdout/stderr is captured, discarded on the expected outcome, and re-emitted **prefixed** (e.g. `check 9 [mutant extractor_self_test]:`) only on an unexpected one.

- **`check_self_invocation` gaining a third parameter ripples through ~10 existing fixtures, and the spec omits the swap case** — the current signature is `check_self_invocation(run_sh_text, scripts)` (`ci_targets.py:539`) and `self_test()` calls it eleven times with two arguments (`ci_targets.py:944-985`). Adding a required third parameter breaks all of them; adding an *optional* one with a benign default (`""`) makes the wired-tree fixture at `:944` fire ("fired on a wired tree") — and a default that vacuously passes re-creates the hole. §4 doesn't say which. Also, §2.4 asserts the extension "follows all three of these conventions" including "the two texts are checked SEPARATELY … so a call site living in the wrong file cannot satisfy the other's requirement," but §4's enumerated fixtures ("a wired tree must not fire; deleting each of the two lines must fire; … only the definition `run_self_tests() {` must fire") contain **no swap case** — while the existing code carries two (`ci_targets.py:980-985`). | Why it matters: a silently-passing third haystack is the same defect class as the one being fixed. | Suggested fix: specify a required third positional parameter, that every existing fixture call site passes a wired actionlint text, and add both swap directions to the fixture list.

- **A *new* self-test that is never wired is still invisible** — the design pins the five known invocation lines. Add a sixth fixture table tomorrow, forget to call it from `run_self_tests`, and nothing notices: `SELF_TESTS_RAN` still equals `SELF_TEST_COUNT`, the battery only mutates lines that are already inside `run_self_tests`, and C4 pins only `run_self_tests`/`selftest_mutation_battery`. That is the SMA-542 defect class, one step out, left open by a spec whose whole premise is "a fixture table that isn't invoked is dead code." | Why it matters: the issue recurs the next time a table is added — the highest-probability future edit. | Suggested fix: add a sixth assertion inside `run_self_tests` (or check 9): count the `^[a-z_]*_self_test\(\) \{` definitions in the file and require it to equal `SELF_TEST_COUNT`. This also removes L2's escape (see below).

- **The `T` floor closes one of two equally cheap silencing edits, but AC-4 claims L6 is closed** — check 8 asserts `:affected-smoke` is a token of `T`. It does not assert that `T` is what `moon ci` runs. `ci.yml:219`/`:221` could become `moon ci "${T[@]}" … || true` and `set -euo pipefail` (`ci.yml:216`) would not save it: the step exits 0, `T` is untouched, `check_invocation`'s `T_ARRAY_EXPANSION` test (`ci_targets.py:80`, `:468`) still matches, and the new floor still passes. Every gate in `T` is silenced by one edit, in the same file and the same line the floor already reads. | Why it matters: AC-4 says the design "clos[es] SMA-541's L6"; it closes the removal spelling of L6 and not the suppression spelling. | Suggested fix: either extend `ci_target_floor_verdict` to assert the `moon ci` line's shape (no `||`/`;` tail after the invocation) — it is already reading that file — or narrow AC-4 and add this to §6 as an explicit named limitation.

- **README claims in §4 and AC-3 do not match the file** — §4 says "A new **Limitations** section replaces the four-line residual" and AC-3 says "`ci/actionlint/README.md`'s stated limitation is replaced by §6." `/Users/smaschek/dev/paigasus/paigasus-core/ci/actionlint/README.md` has no Limitations section and no four-line residual; the only occurrence of "limitation" is line 17, referring to *SMA-525's* L5, which SMA-540 already closed. | Why it matters: an AC an implementer cannot satisfy or verify. | Suggested fix: name the actual lines to change, or state that a Limitations section is being added (not replacing anything). While there: the README's escape hatch at `README.md:86-88` ("**Anything worse**: drop `:actionlint` from `T=(…)`… also removed from the CLAUDE.md `ci-targets` block") is *already broken* by the very mechanism §2.3 leans on — `check_forward` (`ci_targets.py:397-417`) reports it as `missing` unless a `T_EXEMPT` entry is added. The spec discovers this and doesn't fix the doc.

### MINOR

- **`sed`/token matching is unspecified in both directions.** §4 gives no pattern. A loose `/extractor_self_test/d` would also match the definition line `run.sh:273` and the six comment references (`run.sh:14`, `:1137`, `:1188`, `:1275`, `:1378`…), producing either a &gt;2 count (loud, fine) or, for a name appearing exactly twice, a mutant with an orphaned function body that dies of a bash syntax error — killed for the wrong reason. Similarly `no-array` must anchor `^[ \t]*T=\(…\)[ \t]*$` to mirror `T_ARRAY_RE` (`ci_targets.py:66`), not bare `T=(` (which would also match `EXPECT=(`), and `missing &lt;entry&gt;` must compare **whole tokens** (`:affected-smoke` is a prefix of `:affected-smoke-disabled`). Specify all three.
- **L2 is wrong, and understates the design's own coverage.** L2 says replacing `run_self_tests`' body with `:` plus setting `SELF_TEST_COUNT=0` "would not" red. It would: with the body gone, each mutant's `sed` matches zero self-test lines, so "exactly two lines removed" fails and check 9 reds before any subprocess exists. Fix L2, or (better) note the definition-count assertion above closes it outright.
- **D7's "divergence fails safe" argument is circular.** It defends the bash tokenizer against `ci_targets.py`'s C3 — a check inside `repo:affected-smoke`, the gate check 8 exists to guard. Restate the fail-safe argument in terms of check 8's own behaviour only.
- **No verdict for a missing/renamed `ci.yml`.** §4's table covers empty / `no-array` / `missing &lt;entry&gt;`. If `.github/workflows/ci.yml` is renamed, a `grep` on a missing file yields `no-array`, whose remediation text ("keep `T` on one line") is wrong. Add an explicit `no-file` verdict.
- **Temp-dir cleanup is best-effort.** "removed on the way out" under `set -uo pipefail` leaks the `mktemp -d` on any `infra` exit between creation and cleanup. Specify a `trap 'rm -rf "$dir"' EXIT`.
- **Three factual slips in §2/§3.** (a) §2.2 "Two byte-identical blocks" — they are not: `run.sh:1611-1614` is indented four spaces, `run.sh:1868-1871` is not. The conclusion still holds (`check_self_invocation` strips lines, `ci_targets.py:553`), but for a different reason. (b) §1 "`run.sh`'s own header records the principle … from SMA-525's finding F4" — the header is `run.sh:1-23` and does not; the F4 sentence is at `run.sh:1382-1383`. (c) D2 "All four self-test functions are defined by `run.sh:1551`" — `config_self_test` *begins* at 1551 and ends at 1602; the correct bound is `:1602`. All the other cited line numbers (273 / 1283 / 1385 / 1551 / 1604 / 1611-1614 / 1868-1871 / `ci_targets.py:397` / `:539`) check out, as does §2.5's extraction table (verified against `prebuild.yml:10-41`, `security-scan.yml:23-26`, `ci.yml:3-8`).
- **Check 8 is placed behind the `actionlint` PATH guard for no reason.** It reads a tracked file and needs no binary; under D2's snippet a machine without `actionlint` infra-exits at `run.sh:1622` before check 8 ever asserts the floor. Consider placing it with the self-tests.
- **`readonly SELF_TEST_COUNT` buys nothing and costs sourceability.** Under `set -uo pipefail` without `set -e`, a reassignment prints an error and continues; the only real effect is that `source ci/actionlint/run.sh` twice (a plausible future unit-test harness) errors. Keep it if you like, but do not describe it as protection.

### QUESTIONS

- Is check 9 meant to run under `--self-test`? D5 says yes; §4's check-8 wording ("Full gate only") and `README.md:125`'s "for fast iteration" framing say the opposite is wanted. Which?
- What is the accepted wall-clock ceiling? §5 says the README "says so rather than leaving the old number standing" — that is not an acceptance criterion. What number would cause you to reject the battery outright (2×? 5×? 10× of ~1.0s)?
- Why five mutants rather than one mutant plus five textual preconditions? The counter is a single arithmetic mechanism; the spec never argues the marginal four subprocesses discriminate anything.
- What is `selftest_mutation_battery`'s propagation contract? §2.4 establishes that `RUN_SH_CALL_SITES` pins `|| RC=1` suffixes because a bare command prefix let `|| true` masquerade as wired. `ACTIONLINT_SH_CALL_SITES` pins bare names with no suffix — is that because propagation is via the global `FAILED` (as for the four existing self-tests, `run.sh:29-32`)? Say so explicitly, or a reviewer will read it as the hole §2.4 just closed.
- Does the whole-line pin on `run_self_tests` mean a future `run_self_tests || FAILED=1` reds C4? If yes, that brittleness should be in §6.
- D4 asserts C4 is what covers a deleted `run_self_tests` call. Note that check 9 *also* covers it (with the call gone, every mutant's `--self-test` runs nothing and exits 0 → all survive → red). Is that intentional redundancy, and if so should D4 say so — it strengthens the design against the BLOCKER above.

## What's good

- **The cycle (D1) is the correct topology.** Two independently-scheduled gates each asserting the other's scheduling is the strongest structure the graph offers, and L1 correctly and honestly names the residual instead of pretending a third gate would close it.
- **D3 (counter inside the function, not at the call site) is the right choice** and the stated reasoning — survives reformatting, cannot be spoofed by a stranded increment, does not depend on matching source text — is exactly why it beats the grep the issue originally suggested. Keep it, and extend it with the definition-count assertion.
- **§2.3 and §2.4 do real archaeology.** The `check_forward` strict-equality reading is correct (`ci_targets.py:397-417`, `T_EXEMPT = {}` at `:144`, and `repo:actionlint` carries no `runInCI: false` at `moon.yml:481-511`), and the substring-vs-whole-line asymmetry is read off `ci_targets.py:195-207` accurately, including the `run_self_tests` / `run_self_tests() {` prefix trap. D8's vacuity argument for excluding `:actionlint` from the floor is sound and worth preserving verbatim.
- **`ci_target_floor_verdict` mirroring the `pattern_verdict` / `config_verdict` decide-here-print-there split** is the right call — it is what makes check 8 testable by a fixture table at all, and it is the exact lesson `run.sh:994-996` records.</result>
<usage><subagent_tokens>126098</subagent_tokens><tool_uses>33</tool_uses><duration_ms>654985</duration_ms></usage>
</task-notification>