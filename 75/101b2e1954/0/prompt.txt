<task-notification>
<task-id>aa98e545da2626a91</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/072a35f4-ddf1-4d33-9ed9-9df4bba26580/tasks/aa98e545da2626a91.output</output-file>
<status>completed</status>
<summary>Agent "Task 6: docs + full verification" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>STATUS: DONE

Commit SHA: `31220f7a1424adaeeafa3591be9e5b3fa95d886a` — docs(ci): document A7, the cross-stack wrapper-input guard (SMA-560)

Test summary: `ci/affected-graph/run.sh` exit 0 (no missing/unexpected rows, all PASS including 27-target `ci-targets`); full-graph `moon ci` (all 27 targets) exit 0, 60 tasks completed (56 cached) in 3m58s, `paigasus-iam-rs:test` 1037/1037 passed (Docker reachable, canary did not fire).

Concerns: None. No expected set moved, target count stayed 27, full graph passed clean. README A7 bullet added matching A4/A6 depth/voice, "A1-A6"→"A1-A7" range updated, moon-output-assumptions passage extended for A7. Report at `.superpowers/sdd/2026-08-28-sma-560-cross-stack-input-affectedness/task-6-report.md`.</result>
<usage><subagent_tokens>112128</subagent_tokens><tool_uses>44</tool_uses><duration_ms>1099838</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a3a71d230747ff46e</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/072a35f4-ddf1-4d33-9ed9-9df4bba26580/tasks/a3a71d230747ff46e.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 6" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdict

**Spec compliance: ✅**

**Task quality: Approved**

Verified independently (not taking prose on trust):

1. **A7 bullet claims all check out against `cargo_moon_parity.py`:**
   - A6/A7 split confirmed: A6's `check()` loop is unconditional over crates; A7's `check_wrapper_upstream_inputs` (line 506) explicitly skips `language == "rust"`.
   - Task set is DERIVED, not hand-written: line 503, `for target in sorted(derive_ffi_tasks(projects))` — same function A5 uses (line 317 docstring confirms sharing).
   - CONTAINMENT confirmed: A7 only emits `want - observed` (line 547); unlike A6 (`check_rust_closure`, lines 465-473) it never emits the reverse direction, so extra entries (parity-vector corpus, `package.json`/`pyproject.toml`) are correctly tolerated.
   - Both buckets, per-task, non-unioned: `observed = set(files) | set(globs)` is computed inside the `for task in sorted(tasks)` loop (line 536-546) against a single `want` set computed once per wrapper — this is exactly what makes a per-task gap (e.g. `test` missing `build.rs` while `build` has it) visible instead of hidden by unioning.
   - `REQUIRED_WRAPPER_CLOSURE` anti-vacuity floor is edge-based (lines 145-157, 510-524), matching the stated rationale about containment's vacuous-empty-set risk.

2. **Formatting/voice**: the A7 bullet (README.md:117) mirrors the A4/A5/A6 bullet pattern (`**A7** (in \`cargo_moon_parity.py\`, SMA-560) is A6's cross-stack twin:` — same construction as A5's `is A4's cross-stack twin (SMA-546)`).

3. **Range reference**: "A1-A6" → "A1-A7" updated at README.md:181.

4. **moon-output-assumptions passage**: extended at both call sites (lines 65-66, 292-300) to name A7's reliance on `inputFiles`/`inputGlobs`/`command`/`args`/`script`/`language`, correctly noting it introduces no new assumption beyond A4-A6.

5. `grep -c A7 ci/affected-graph/README.md` — confirmed 6 matches, as the report claims.

6. **Global constraints**: diff touches only `ci/affected-graph/README.md`; no `.moon/`, `ci.yml`, or CLAUDE.md changes. Commit `31220f7` header (`docs(ci): document A7, the cross-stack wrapper-input guard (SMA-560)`, 69 chars) is lowercase-after-colon, scope `ci` is valid, under 100 chars; no offending body line.

**Minor note (non-blocking):** the code docstring adds a nuance the README omits — one hand-written `run.sh` case (`kernel-&gt;consumer-tasks`) already covered the kernel→wrapper edge before A7. The README's "asserted by nothing" is scoped to A6/this script and is the exact framing the controller's checklist required, so this is not a misstatement, just slightly less complete than the source docstring.</result>
<usage><subagent_tokens>72546</subagent_tokens><tool_uses>9</tool_uses><duration_ms>80781</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a76cb2a1a27903720</task-id>
<tool-use-id>toolu_01VJjjV4336owos5WHSvwVSe</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/072a35f4-ddf1-4d33-9ed9-9df4bba26580/tasks/a76cb2a1a27903720.output</output-file>
<status>completed</status>
<summary>Agent "Final whole-branch review" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdict

**Do not merge yet.** The substance of the branch is correct and well-argued — the two Rust input gaps are closed at the right layer, A7's design (derived task set, containment, both buckets per task, edge-based floor) is the right shape, and the two live wrapper under-declarations are genuinely fixed. But four of the branch's own anti-vacuity guarantees are not held by any durable control, and **three of them I measured green under a one- or two-character mutation of the very code they name**. That is this repo's signature defect, in new code, at the final gate. All four fixes are small and local to `self_test()` plus one signature change.

I restored every mutation; `git diff` is empty and both `--self-test` and the real run are green.

---

## Important

**I1 — A7-c is a vacuous control** (`ci/affected-graph/cargo_moon_parity.py:820-825`)

MEASURED: delete `or proj.get("language") == "rust"` from the examined-set filter (`:506`) and `--self-test` still prints `all seven assertions fire` and exits 0. The `rusty` fixture is a *clean* one — `want ⊆ observed` holds whether or not `k-ts` is examined — so `!= []` cannot distinguish "filtered out" from "examined and satisfied". This is the same defect commit `e7bea74` fixed one control below it in A7-e/A7-f.

Harm if the filter is deleted is low today (no Rust project matches an FFI marker — I confirmed the real derived set is exactly the three wrapper tasks), so this is about the control, not a live hole. Fix: make the Rust-flipped fixture under-declared, e.g. `rusty["k-ts"]["task_input_globs"]["build"] = []` before the call, so examining it would emit rows.

**I2 — nothing durable holds A7's per-`(project, task)` read** (`:773-818`, the `wrap` fixture)

Spec §D5 names this as one of A7's three deliberate differences, and states that a union-across-tasks read "also passes" the headline mutation. MEASURED: replace the per-task lookup at `:536-538` with a union over all of the wrapper's tasks and `--self-test` stays green — the `wrap` fixture declares exactly one task (`build`), so the property is invisible to it by construction. The controller's real-graph mutation (wasm glob off `:test` → one row on `:test`) proves the property holds *today*, but a one-off measurement is not a control; a future refactor would pass CI. Fix: give `k-ts` a second `test` task, complete on `build` and missing one entry on `test`, and assert the row names `k-ts:test`.

**I3 — `root=None` silently un-asserts A7's `build.rs` half** (`:477`, `:534`; call site `:1213`)

MEASURED, three steps: change `check_wrapper_upstream_inputs(projects, root=root)` to `(projects)` → `--self-test` green **and** the real run green; then delete both `/rs/crates/bindings/paigasus-node-bindings/build.rs` lines from `ts/packages/paigasus-kernel/moon.yml` → the real run is **still** green. (With `root` intact that same deletion reds, rc 1 — so the assertion does work.) The branch's own headline SMA-537×SMA-560 fix is therefore unpinned against a two-character edit, and the default is what makes it silent rather than loud. Fix: make `root` a required parameter (no default). That also closes deferred minor #3 if you add a `tempfile.TemporaryDirectory()`-backed self-test row that writes a real `build.rs` and asserts the row fires.

**I4 — the "one list" restructure is weaker than its comment claims** (`:1203-1207`)

The comment says "Appending here is now the only way to add a check, and forgetting to means it is never called at all." That is true only for a check whose call is *inline* in `findings` **and** whose function name appears nowhere else in `main` — i.e. A6 and A7 only. MEASURED twice:

- delete the `(a5, …)` tuple → `--self-test` green, and the real run exits **0** with a genuine A5 violation present (I removed `/.prototools` from `paigasus-kernel-py:test`; baseline rc was 1);
- delete the `(check_task_inputs(projects, crates, "fmt", FMT_TASK_INPUTS), …)` tuple → same, with a genuine fmt violation present (I removed `/rs/rustfmt.toml` from the `fmt` inputs).

Cause: the `unreferenced` guard (`:756-764`) only requires the *name* to appear once in `main`'s source. `a5` is pre-computed at `:1195`, and `check_task_inputs(` appears twice, so either entry can be dropped in one edit. `a1/a2/a3` are worse — `check` does not even match the `check_` prefix filter.

This matters doubly for `a5`: A7 deliberately has **no task-set floor of its own** (`:145-147`) because it relies on `REQUIRED_FFI_TASKS`, whose rows are reported through the `a5` entry. Dropping `a5` therefore un-floors A7's derived task set as well.

Not a regression — pre-branch, removing `a5` from the guard expression was equally one edit — but the commit message asserts the problem is solved, and it is solved for two of seven assertions. Fix: an explicit arity/text floor over `findings` in `self_test()` (precedent: `SELF_TEST_COUNT`, `SELF_SCHEDULED_GATES` whole-line pins in `ci_targets.py`), since a name-based guard structurally cannot separate the two `check_task_inputs` call sites.

---

## Minor

**M1 — README repeats the rev-1 error** (`ci/affected-graph/README.md:120`). "those wrappers were asserted by nothing" omits `run.sh:343`'s `kernel-&gt;consumer-tasks` case, which already covers the kernel→wrapper edge. The code docstring (`:481-484`) states it correctly. A reader taking the README alone re-derives exactly the claim an adversarial pass caught in the spec. One clause fixes it. **Fix before merge.**

**M2 — stale function name** (`cargo_moon_parity.py:86`) — the comment names `check_lint_inputs`, which the same commit renamed to `check_task_inputs`. **Fix before merge** (one word, same file).

**M3 — `check_task_inputs`'s suffix fallback is discriminated by the wrong predicate** (`:274`). Answering the brief directly: **it does not loosen the lint case** — all three `WORKSPACE_LINT_INPUTS` start with `rs/`, so the fallback is unreachable for lint, and the generalization is in fact marginally *stricter* than `check_lint_inputs` (it now also fires when `inputGlobs` is absent). Two latent weaknesses remain: (a) the predicate is `not f.startswith("rs/")` where the intent is "crate-relative", so a future required entry outside `rs/` — `.prototools`, `contracts/...` — silently gains suffix matching; (b) the suffix is not anchored to the crate under examination, so `a-rs:fmt` would be satisfied by `rs/crates/libs/b/src/**/*`. Both close with one anchored comparison against the crate's own `source_dir` — I verified moon resolves these to exactly `&lt;source_dir&gt;/src/**/*` — which also lets the `rs/` special case go away.

**M4 — the `fmt` comment names `benches/` but the inputs do not cover it** (`.moon/tasks/rust.yml:94`). `cargo fmt --check` does format `benches/` and `examples/`; `@group(tests)` is `tests/**/*` + `**/*_test.rs`. No such directory exists today, so nothing is broken — but the comment's enumeration reads as a coverage claim. Either add the two globs (they match nothing, exactly as `build.rs` does for twelve crates) or say they are out.

**M5 — one genuinely uncovered upstream file, outside this branch's scope.** Answering "is any other upstream file a wrapper task reads but not keyed on": the closures are minimal and complete (`kernel-py` → kernel + py-bindings; `kernel-ts` → kernel + node-bindings + wasm), and every `src/**/*`, `Cargo.toml`, `build.rs`, `package.json` and `pyproject.toml` in them is declared. The one file that is read and keyed on by *nothing* in the repo is **`rs/.cargo/config.toml`** — it supplies the apple-darwin `-undefined dynamic_lookup` link flags the cdylibs need, and the ts wrapper's own comment (`ts/packages/paigasus-kernel/moon.yml:24`) says so. It is `[target.*-apple-darwin]`-scoped, so Linux CI is unaffected and this is a local cache-correctness issue only. Follow-up issue, not this branch. (I also considered `/py/pyproject.toml` and `/ts/pnpm-workspace.yaml`; both move in lockstep with the lockfiles already declared, so I do not rate them.)

---

## Deferred-minor triage

1. `:86` stale `check_lint_inputs` → **fix before merge** (M2; one word, same commit renamed it).
2. `check_ffi_inputs` flat-string sort → **defer**. Verdict-neutral, and the precondition (one project id a strict prefix of another) does not exist in the graph.
3. A7's `build.rs` branch unreachable from `self_test()` → **fix before merge**, folded into I3. It is not merely untested coverage; combined with the `root=None` default it is the reason I3 is silent.
4. A6's floor controls → **defer**, but the logged shape is slightly off. A6-e is *not* loose: its fixture (a-rs present, `language: rust`, `deps` emptied) can only trip the derivation branch, and the second control discriminates on `"A6 examines"`. The actual gap is that A6's `pid not in projects` branch has **no** control — MEASURED: deleting it leaves `--self-test` green. Harm is low (an absent consumer still reds via the not-in-examined branch, with a misleading message). A7-f now covers the equivalent branch, so the asymmetry is worth a follow-up.
5. README "asserted by nothing" → **fix before merge** (M1).

---

## What I checked and found sound

- **Spec delivery:** every section maps to shipped code. D1/D2 (`fmt` inputs, `build.rs` in the shared `sources` group), D3 (`check_task_inputs` spanning both buckets), D4 (derived task set shared with A5, plus the `derive_ffi_tasks` divergence control at `:788-793`), D5 (containment, file-granular, both buckets, edge floor, floored floor, absent-entry rule), §4.3 (both wrappers fixed in the same commit as A7), §4.4 (the `main()` restructure — see I4 for the caveat). No spec requirement is missing.
- **Containment vs. strict equality is justified.** I could not construct a real defect it admits beyond documented L1. The wrappers legitimately declare `kernel-parity/vectors/**`, `paigasus-node-bindings/package.json` and `paigasus-py-bindings/pyproject.toml` under `rs/crates/`; strict equality would report all three as over-approximation, i.e. record a correct input as a defect. The dangerous direction (a missing entry) is caught, and A5's floor plus `REQUIRED_WRAPPER_CLOSURE` cover the two ways `want` could empty (verified: the closure-broken, not-examined and absent-project branches all fire and all name themselves).
- **A6 is not disturbed by `build.rs` in `sources`.** Confirmed against the real graph: every crate's `fmt`/`build` reports `&lt;own&gt;/build.rs` in `inputFiles`, and A6's `observed` filter excludes `own/`-prefixed entries. Also confirmed the `inheritedBy: languages: ['rust']` scope holds — the only `build.rs` entries on non-Rust projects are the two intentional new ones on `paigasus-kernel-ts:{build,test}`.
- **The twelve non-existent `build.rs` inputs are inert.** `repo:input-liveness` is `repo:*`-scoped (confirmed in `task_inputs.py`), so spec L3 is accurate.
- **A language flip on a wrapper is loud, not silent** — A7's floor fires (`pid not in examined`) *and* A6 picks the project up and reds on strict equality.
- Comment quality is high throughout and states the reasoning a future engineer needs; I have no length complaints.</result>
<usage><subagent_tokens>149877</subagent_tokens><tool_uses>38</tool_uses><duration_ms>573699</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ad7cbacf8911dc494</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/072a35f4-ddf1-4d33-9ed9-9df4bba26580/tasks/ad7cbacf8911dc494.output</output-file>
<status>completed</status>
<summary>Agent "Final review fix wave" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>**Status:** all six findings fixed; none was wrong on inspection. I first reproduced I1 and I4 green on the pre-fix code, then measured each mutation red after.

**Commit:** `a0ffc17` — `ci(repo): close four vacuous controls in the A7 branch (SMA-560)`

**Tests:** `--self-test` rc 0, real parity check rc 0 (13 crates), `ci/affected-graph/run.sh` rc 0. Mutation drills all now red: drop language filter → A7-c fails; union across tasks → A7-g fails; neuter the `build.rs` clause → A7-h fails; drop the `a5`, `a4-fmt`, or `a1` tuple → the key floor fails; restore the unanchored tail match → the new A4 row fails. Omitting `root` is a `TypeError`. Real-graph drill: deleting both `build.rs` lines from `ts/packages/paigasus-kernel/moon.yml` now reds the real run with rows for `paigasus-kernel-ts:build` and `:test`.

**Concerns:**
- I4 needed a structural change: the findings list moved into a module-level `collect_findings()` returning `(key, rows, title)`, pinned by `EXPECTED_FINDING_KEYS`. `main()` now calls it inside the `INFRA_ERRORS` try, so a `MoonOutputError` from A6/A7 maps to rc 2 instead of crashing — a behavior improvement, but a change.
- The A7 self-test block is now wrapped in a `TemporaryDirectory`, so its rows are indented one level deeper; the diff is larger than the fix is.
- I added one sentence to the README A7 bullet beyond M1's ask, documenting the required `root` and the `build.rs` demand.
- Full 27-target `moon ci` not run, per your instruction.

Report at `.superpowers/sdd/2026-08-28-sma-560-cross-stack-input-affectedness/final-fix-report.md`. Tree clean apart from the pre-existing untracked `.claude/`.</result>
<usage><subagent_tokens>126171</subagent_tokens><tool_uses>48</tool_uses><duration_ms>656888</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a218495a575e36ff8</task-id>
<tool-use-id>toolu_01LbdhrLDTbYqd1yi6yqCMAe</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/072a35f4-ddf1-4d33-9ed9-9df4bba26580/tasks/a218495a575e36ff8.output</output-file>
<status>completed</status>
<summary>Agent "Scoped re-review of the fix wave" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdicts

**I1 — ADDRESSED.** `rusty` fixture now empties both `task_inputs` and `task_input_globs` for both tasks, so an examined `k-ts` must emit rows. `!= []` now discriminates. Non-vacuous.

**I2 — ADDRESSED.** `wrap` carries `build` + `test` with different complete sets, both matching an FFI marker. A7-g asserts row equality `"k-ts:test inputs omit rs/crates/libs/kern/src/**/*"` **and** that no `k-ts:build inputs omit` row appears — both halves needed to kill a union refactor. A7-e's fixture change (both invocations set to `echo nothing`) is required by the second task, not a weakening.

**I3 — ADDRESSED.** `root` required; `root is not None` gone. A7-h builds a real tree, asserts the exact row for `build` **and** `test`, plus the negative (no `build.rs` on disk ⇒ no demand). Specific substrings throughout.

**I4 — ADDRESSED.** Measured on mutated copies: drop the `a5` tuple → rc 1; drop `a1` → rc 1; empty the list → rc 1; swap two keys → rc 1. The pin asserts non-emptiness of `EXPECTED_FINDING_KEYS`, arity, **and** exact key sequence (so membership and order). The overclaiming comment is corrected in `collect_findings`'s docstring.

**M1 / M2 / M3 / M4 — ADDRESSED.** M3's anchoring is `f"{info['source_dir']}/{f}" in observed`, with a new row asserting `a-rs:fmt` is not satisfied by `b`'s globs; the `rs/` special case is gone and workspace-relative entries keep verbatim-only matching for free. M4 reworded, no globs added — `find rs -type d -name benches -o -name examples` hits only `rs/target/**`, so the claim holds.

## Structural change

**Exit codes: all three correct.** Measured directly by stubbing `moon_projects`: a `MoonOutputError` raised from *inside* `collect_findings` → rc 2 with the `FATAL [parity]` line; a fixture with real under-declarations → rc 1 with A5/A6/A7 sections; rc 0 clean (controller-verified).

**No assertion can be misreported as infra.** Every check returns rows; none raises. The only exception source inside the new try-scope is `derive_ffi_tasks`' `MoonOutputError`, which A5 already raised from inside that same try before this diff — so the rc-2 surface widens by exactly zero triggers in practice, and the widening it does describe (A7 reaching the same raise) is correctly classified as infra. `Path.is_file()` swallows `OSError`, so the new disk touch cannot leak into `INFRA_ERRORS` either. `check`, `check_task_inputs`, `check_upstream_inputs` do no I/O.

**`collect_findings()` module-level is safe.** `self_test` calls the same module global `main()` calls — there is no second list. It also feeds `inspect.getsource(collect_findings)` into the `unreferenced` name scan, which is why emptying the list is caught twice over.

**Re-indentation is mechanical.** Indentation-normalised diff of the A7-a…A7-e block shows 28 changed lines, all accounted for: threading the `bare` root argument, the I1 under-declaration, one comment rewrap, and A7-e's two-task invocation dict. No assertion substring was loosened.

**No production message reworded.** Diffing every message-emitting line outside `self_test` yields one line, moved, not edited. The one message change (`"main() never calls"` → `"the real run never calls"`) is a self-test failure string, and it is now accurate.

## New breakage

None Critical or Important.

**Residual (Minor, inherent to registry pins).** Deleting the `a5` tuple **and** its key from `EXPECTED_FINDING_KEYS` in one edit is still rc 0 — measured. The `check_` name scan cannot see it because `a5 = check_ffi_inputs(projects)` remains in `collect_findings`. This is the same bounded shape as `SELF_SCHEDULED_GATES` and the `T`-array cycle; the comment already says "adding a check means adding its key here AND its tuple there", but does not name the deletion residual. Optional one-line comment, not a blocker.

**Nit (Trivial).** The diff removed one of the two blank lines before `if __name__`. `ruff` is scoped to `py/`, so `ci/` is ungated and no CI target reds; sibling scripts use two.

## Merge verdict

**APPROVE.** All eight findings addressed with non-vacuous, substring-specific controls. The structural change is a net improvement: the findings list gained a real floor that bites on four distinct mutations, and the rc-2 widening is correctly scoped with no path by which a violation becomes "could not evaluate".</result>
<usage><subagent_tokens>87626</subagent_tokens><tool_uses>15</tool_uses><duration_ms>194706</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>bs6ewtj7l</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/072a35f4-ddf1-4d33-9ed9-9df4bba26580/tasks/bs6ewtj7l.output</output-file>
<status>completed</status>
<summary>Background command "Final full-graph verification after the fix wave" completed (exit code 0)</summary>
</task-notification>

---

create the PR and stop at gate 2