<task-notification>
<task-id>a3447ef7b7f6c6dab</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-core/b9b99dc1-235a-4cbd-899a-f0194d3bde9f/tasks/a3447ef7b7f6c6dab.output</output-file>
<status>completed</status>
<summary>Agent "Adversarial spec challenge" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdict: NEEDS REWORK

The core shape is right — D1 (prepend, don't add a fourth task) and D3 (`set -euo pipefail`, control first) are correctly reasoned and match repo precedent, and the issue's premise survives scrutiny (I found a concrete mutation the control catches and the real run cannot). But D2's stated justification is factually wrong, D4's relaxation is pointed in the direction that weakens exactly the follow-up the spec advertises, and — decisively — adding three keys to `SELF_SCHEDULED_GATES` silently de-fangs roughly 25 negative assertions in `ci_targets.py --self-test`, which the spec waves at in one clause of Verification 6. The rework is narrow but it is design work, not editing.

## Findings

### BLOCKER

- **Adding keys to `SELF_SCHEDULED_GATES` makes `ci_targets.py --self-test` vacuous, and the obvious fix does not fix it** - Every negative fixture in `self_test()` asserts `if not check_self_invocation(...)`. Once `SELF_SCHEDULED_GATES` has four keys, *every* call whose `scripts` argument lacks the three `release-parity*` entries returns non-empty **regardless of the mutation under test**, so all of them pass for the wrong reason. Two populations: (a) the ones that pass the shared fixture `scripts` (`/Users/smaschek/dev/paigasus/paigasus-core/ci/affected-graph/ci_targets.py:1074`, `scripts = {"input-liveness": wired_script}`) — lines 1107, 1110, 1113, 1149, 1152, 1160, 1171, 1182, 1193, 1208, 1215, 1224, 1232, 1240, 1248, 1263, 1265; (b) the ones that pass a **literal one-key dict** — lines 1118, 1122, 1130, 1134, 1138, 1142, 1267. Only the positive control at :1101 (`fired on a wired tree`) reds, and it reds loudly — so an implementer following Verification 6 ("update the self-test fixtures if the new keys break them") extends line 1074, sees green, and ships with population (b) — including the `prefix hole` fixture at :1118 that is the *only* proof D4's whole-line matching works — permanently vacuous. | Why it matters: this is the exact vacuity class the repo has burned three PRs on (SMA-526, SMA-542 I1, CodeRabbit round 4 C1), and it would be introduced by the PR whose stated purpose is preventing rotted self-tests. | Suggested fix: spec a fixture *builder* rather than literals — e.g. `def wired_scripts(**overrides)` returning a fully-wired dict for **all** entries of `SELF_SCHEDULED_GATES`, with each negative fixture mutating exactly one gate's script; add a per-gate positive control (a fully-wired `release-parity-ts` script must not fire) and a per-gate deletion fixture for each of its three lines. State this in the spec, not as a conditional in Verification.

### MAJOR

- **D2's rationale is false; the conclusion may survive but not for the reason given** - The spec claims "a control on `release-plz` says nothing about whether the semantic-release adapter's `ecosystem::version` still reads a real version." It says exactly that, five times, with three distinct expected values: `run.sh` line 74-83 drives the same `build_fixture`/`apply_commit`/`run_update`/`version` quartet per case. The control's per-ecosystem code path is a **strict subset** of the real run's — one `check_case` invocation with `expected="0.1.1"`, which is also `cases.tsv`'s first row's expectation. Everything the control uniquely proves lives in tool-agnostic code: the comparison at `ci/release-parity/run.sh:51-57` and the control block at `:60-69`. The driver/fixture/config table in D2 lists real differences that the real run already exercises identically. | Why it matters: D2 is the decision that triples the cost and triples the pinned-string surface; it must not rest on a claim the code contradicts, or the next reviewer will "simplify" it back on correct evidence. | Suggested fix: re-argue D2 honestly — cost (~1s each), symmetry (nobody has to remember which task carries the control), and the forward-looking case (if `check_case`'s comparison is ever specialised per ecosystem, a single control silently under-covers). Delete the `ecosystem::version` claim.

- **D4's relaxation direction removes the forcing function on the follow-up the spec itself advertises** - `SELF_TASK_EXPECTED_GLOBS ⊆ SELF_SCHEDULED_GATES` permits a script-pinned gate with no inputs pin. The Non-goals section names `affected-smoke` as a one-line follow-up — and `repo:affected-smoke`'s inputs are the most load-bearing input list in the repo (`moon.yml:130-162`; the `ci/actionlint/**/*` entry carries an explicit "Do not remove that input", and `CLAUDE.md`, `.prototools` and `moon.yml` each carry their own reachability comment). Under the proposed subset test, adding `affected-smoke` to `SELF_SCHEDULED_GATES` without pinning those globs is silent. Worse, the chosen direction still **forbids** the thing you would most want today: a globs-only entry for `affected-smoke` (to protect D5's reachability, see below) without a script pin. | Why it matters: the relaxation is sold as "makes adding them a one-line change later"; it actually makes adding them a one-line change *that skips the harder half*. | Suggested fix: keep both registries independent but require a recorded exemption in the repo's own idiom (`T_EXEMPT`, `ALLOW_DEAD_INPUT`, `ALLOW_NO_CARGO_BACKING`, `BRANCH_SKIP`, `COE_SKIP` all do this): `SELF_TASK_GLOBS_EXEMPT = {"release-parity": "&lt;reason&gt;", ...}` with a non-empty-reason assertion and a stale-entry check, exactly like `check_forward`'s `bad_exempt`/`stale_exempt` at `ci_targets.py:531-532`.

- **D5's reachability rests on an unpinned input, and nothing keeps these three tasks in CI at all** - Two gaps. (1) The pin fires only because `repo:affected-smoke` lists `- 'moon.yml'` (`moon.yml:134`). Nothing asserts that entry. Deleting it is itself a *root* `moon.yml` edit, and post-edit the task's remaining inputs do not match the root file (`*/moon.yml` matches `rs/moon.yml`, not `moon.yml`; `.moon/**/*` does not match it) — so the removal PR would not schedule the gate, and every subsequent PR could delete the three control lines silently. (2) Nothing pins `release-parity*` as CI-eligible: `REQUIRED_REPO_TASKS` is `("affected-smoke", "promtool", "publish-metadata")` (`ci_targets.py:151`), and `check_forward` (`:513-533`) computes `want = eligible - exempt` and `got = T ∩ repo`, so removing all three from `T` **and** setting `runInCI: false` (or deleting the tasks from `moon.yml`… though that path *is* caught, since `scripts.get(task, "")` goes empty and the pin fires) shrinks both sets consistently and passes green. | Why it matters: the spec's own premise is "a self-test that runs only when a human remembers is not a control"; a control that runs only while someone leaves the task in `T` is the same shape. | Suggested fix: add the three names to `REQUIRED_REPO_TASKS` (and update the `check_floor` fixture at `ci_targets.py:917-921`, which asserts the exact missing list), and either pin `affected-smoke`'s `moon.yml` input via the decoupled globs registry or record it as a named Limitation.

- **The thing being guarded can be deleted with everything green** - `ci/release-parity/run.sh:14` parses `--negative-control` into `NEGATIVE`; `:60-69` is the block that acts on it. Delete the block only and leave the flag parse: `run.sh --negative-control` falls through to the real suite, exits 0, all three tasks stay green, and CI silently runs the five-case suite twice per task. The proposed pin sees nothing — it pins the `moon.yml` text, not the semantics. And a `ci_targets.py` pin on `run.sh`'s block would be **unreachable**, because `repo:affected-smoke`'s inputs (`moon.yml:130-162`) do not list `ci/release-parity/**` — the identical trap SMA-542 had to fix by adding `ci/actionlint/**/*`. | Why it matters: the spec closes the `moon.yml` half of the guard-the-guard gap and never names the `run.sh` half, in a repo whose CLAUDE.md and memory both single out "a new gate check's OWN call site is what goes unguarded". | Suggested fix: either close it (add `ci/release-parity/**/*` to `repo:affected-smoke`'s inputs plus a `RELEASE_PARITY_SH_CALL_SITES`-style whole-line pin on `if [ "$NEGATIVE" = 1 ]; then` and the two verdict `echo` lines) or record it as an explicit, reasoned Limitation in the README D6 is already writing. Do not leave it unstated.

- **A materially simpler alternative is never considered or rejected** - Have `run.sh` itself run `check_case` with the wrong expectation before the suite (keeping `--negative-control` as the standalone entry point). Cost is identical (same two passes), and it deletes D4, D5 and D6 outright: no `moon.yml` edit, no three new pinned strings, no relaxation of the `ci_targets.py` pairing invariant, no README re-key trick — and the control is re-keyed by `ci/release-parity/**/*` **forever** rather than once. The honest argument against it is guard-the-guard (a control inside the script it tests dies in one edit) — but per the finding above, the proposed design has that same hole one level down, so its marginal guard value is "you cannot delete the invocation" while "you can still delete the block it invokes". | Why it matters: three of the six decisions exist only to serve a choice that was never compared. | Suggested fix: add a decision that states the alternative and rejects it on stated grounds, or adopt it.

### MINOR

- **D4's prefix-direction claim is wrong, and the argument order is never fixed** - "for the `-py` and `-ts` tasks the real-run line … is a strict prefix of the control line … so the same hazard runs the other way" — that is the *same* direction as the base task (real ⊂ control) in all three cases; there is no other way. Harmless to the conclusion, but it means the strings were never enumerated: the spec never says whether `-py`'s control is `run.sh --ecosystem python-semantic-release --negative-control` or `run.sh --negative-control --ecosystem python-semantic-release`, and the pin is byte-exact. | Suggested fix: write all nine strings verbatim into the spec and drop the "both directions" framing.

- **Whole-line pins are brittle in the false-red direction and that is not recorded** - The base task's pinned `ci/release-parity/run.sh` reds if anyone makes the ecosystem explicit (`--ecosystem release-plz`), adds a trailing comment, or reorders flags. `ACTIONLINT_SH_CALL_SITES` documents exactly this consequence for its own entries (`ci_targets.py:288-294`); the new entries deserve the same sentence.

- **The task-script haystack's known residual is tripled without mention** - `check_self_invocation` strips **both** sides for task scripts (`ci_targets.py:673`, rationale at `:274-277`), so an indented copy inside `if false; then … fi` satisfies the pin — the case the actionlint haystack's column-0 rule exists to reject and which is unavailable here. Also `set +e` inserted after the pipefail line satisfies all three pins while re-opening precisely D3's masking. One sentence in `ci/release-parity/README.md`'s new section.

- **`ci/affected-graph/README.md:124` goes stale and nothing gates it** - It reads "`SELF_SCHEDULED_GATES`, whole-line-matched — currently `repo:input-liveness`'s, SMA-553", and the maintenance paragraph at `:137-149` describes the registry pairing. Neither is in the spec's Files-touched table (spec:223-229). Consider whether CLAUDE.md's gotchas need a line too — it carries the SMA-542/SMA-553 equivalents.

- **The measurement table's PSR row is internally inconsistent** - `python-semantic-release`: control (1 case) 3s, real (5 cases) 3s. Either a mis-measurement or the number is entirely fixed overhead (`uv run --frozen python -c …` at `ecosystems/python-semantic-release.sh:28`, executed once per `run.sh` invocation, plus two `semantic-release version --print` calls per case). The other two rows imply ~1.0-1.2s/case, which would put PSR's real run at ~8s, not 3s. Also: measuring "control" and "real" as two independent **warm** local runs misattributes cold cost — after this change the control runs first and pays `proto bin release-plz`, the `py/.venv` bootstrap and node module resolution that the real run pays today. | Suggested fix: report one net task delta per task (`moon run repo:release-parity-py --force`, before vs after), not two independent numbers. The conclusion ("cheap") almost certainly survives, but "~5s total" hides that `-py` is a 100% increase on the spec's own figures.

- **The control's literal expectation is coupled to `cases.tsv`'s contract** - `run.sh:62-63` hardcodes `0.1.1` as "deliberately wrong" for `fix!` in 0.x. If the canonical contract ever changes so that is correct, all three controls red spuriously and the diagnosis is non-obvious. Non-goals exclude touching `cases.tsv`, but the coupling belongs in the README D6 already commits to writing.

### QUESTIONS

- What exact argument order do the `-py`/`-ts` control invocations use? The pin is byte-exact and the spec elides it behind `(...)`.
- Should the three tasks join `REQUIRED_REPO_TASKS` as part of this change, given the control's durability is bounded by the task's presence in `T`?
- Was the control ever run on a GitHub runner? Specifically: cold `py/.venv` (there is no separate `uv sync` step, per `python-semantic-release.sh:23-28`), cold proto-managed `release-plz`, cold pnpm store for the node runner.
- The control now runs first under `set -e`, so an rc-2 INCONCLUSIVE (`run.sh:67`) kills the task before the real gate runs. Confirm that is intended — I believe it is a non-regression (the same infra error would surface from the real run), but it changes which diagnostic appears first.
- Is the `affected-smoke`/`publish-metadata`/`error-code-single-site` follow-up actually filed, and does it include pinning `affected-smoke`'s inputs (not just its script)?
- For `-py` and `-ts`, slot `b`'s "stayed at baseline" assertion is satisfied by "the tool produced no version at all" (`python-semantic-release.sh:183-189`, `semantic-release.sh:156`). I believe this is sound (no release *is* baseline, and PSR guards the fallback with a `git log` check), but Non-goal 2 asserts the control needs no strengthening without addressing it — confirm.

## What's good

- **The premise holds, and I tried to break it.** Concrete mutation the control catches and the real run cannot: change `ci/release-parity/run.sh:51` from `if [ "$got_a" = "$expected" ] &amp;&amp; [ "$got_b" = "$BASELINE" ]` to `if [ "$got_b" = "$BASELINE" ]`. Slot `b` is at baseline in all five `cases.tsv` rows, so the real run prints `== all parity cases passed ==` and exits 0; the control gets rc 0 from `check_case` and reds with "harness accepted a wrong expectation". That is a plausible refactor shape, and it validates the whole issue. Keep this in the spec — it is stronger evidence than the SMA-528 analogy.
- **D3 is exactly right**, including the subtle half: `run.sh`'s own `set -euo pipefail` governs its body, not the Moon block that invokes it twice, and pinning the pipefail line as a first-class required string (rather than decoration) mirrors the reasoning already recorded at `ci_targets.py:199-209`. Do not lose this in revision.
- **D1's cost accounting for a fourth task is accurate** — a new `repo:*` gate really does cost a `T` entry, a matching edit inside CLAUDE.md's marker block (enforced token-for-token and in order by `check_docs`, `ci_targets.py:547-564`), and the measurement-before-analogy discipline the issue asked for is the right instinct even though the PSR row needs re-taking.
- **D6's re-key mechanism is real**: all three tasks list `ci/release-parity/**/*` (`moon.yml:62, 71, 83`), which does match top-level files in that directory — the same premise SMA-542's `ci/actionlint/**/*` reachability fix depends on. Insisting the PR exercise its own change is the right standard.</result>
<usage><subagent_tokens>143572</subagent_tokens><tool_uses>25</tool_uses><duration_ms>619238</duration_ms></usage>
</task-notification>